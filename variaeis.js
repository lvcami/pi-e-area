let raio = 5; // definindo o raio
let area = Math.PI * raio * raio; // calculando a area
console.log(area); // imprime a area
raio = 10; // alterando o valor do raio
area = Math.PI * raio * raio; // recalculando a area
console.log(area); // imprime a nova area